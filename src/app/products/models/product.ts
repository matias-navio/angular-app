export class Product {

    // elsigno de excalamación en los atributos indica que luego se le van a definir valores
    id!: number;
    name!: string;
    description!: string;
    price!: number;
}